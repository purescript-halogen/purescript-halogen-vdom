{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf",
    "test": "pulp build -I test -- --censor-lib --strict",
    "build": "pulp -w build"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript": "^0.13.8",
    "rimraf": "^2.6.2"
  }
}
