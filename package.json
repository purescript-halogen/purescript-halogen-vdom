{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf",
    "test": "pulp build -I test -- --censor-lib --strict",
    "pretest:bundle": "npm run test",
    "test:bundle": "pulp build -m Test.Main --to app.js",
    "build": "pulp build -- --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.1",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.5.4"
  }
}
